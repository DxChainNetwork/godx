package filesystem

// metadataUpdate is the update to update the dxdir metadata.
// It will collect the information and modify the metadata of the dxdir
// recursively all way up to the root directory
type metadataUpdate struct {
}
